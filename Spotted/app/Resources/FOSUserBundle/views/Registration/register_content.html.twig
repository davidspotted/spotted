{% trans_default_domain 'FOSUserBundle' %}

<form id="register-form" action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="home-form">
    {{ form_widget(form) }}
 <!-- 
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
 -->
</form>
